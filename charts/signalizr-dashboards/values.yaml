# Enable standalone dashboard rendering.
enabled: true

# Grafana folder selected by the sidecar's grafana_folder annotation.
dashboardFolder: Signalizr

# Datasource UIDs injected through exact string replacement.
datasources:
  prometheus: prometheus
