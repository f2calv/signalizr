name: deploy-dashboards

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - charts/signalizr-dashboards/**
      - .github/workflows/deploy-dashboards.yml

permissions: {}

jobs:
  version:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    outputs:
      chart-version: ${{ steps.chart.outputs.version }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 1

      - name: read dashboard chart version
        id: chart
        shell: bash
        run: |
          VERSION=$(awk '$1 == "version:" { print $2; exit }' charts/signalizr-dashboards/Chart.yaml | tr -d '"')
          if [[ ! "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+([-.][0-9A-Za-z.-]+)?$ ]]; then
            echo "::error file=charts/signalizr-dashboards/Chart.yaml::Invalid chart version '$VERSION'."
            exit 1
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: jq empty dashboard JSON
        shell: bash
        run: |
          for dashboard in charts/signalizr-dashboards/dashboards/*.json; do
            jq empty "$dashboard"
          done

  package:
    needs: version
    uses: f2calv/gha-workflows/.github/workflows/helm-chart-package.yml@v1
    permissions:
      contents: read
      packages: write
    with:
      tag: ${{ needs.version.outputs.chart-version }}
      image-registry: ghcr.io
      image-repository: ${{ github.repository_owner }}/signalizr
      chart-registry: ghcr.io
      chart-registry-username: ${{ github.repository_owner }}
      chart-repository: ${{ github.repository_owner }}/charts/signalizr-dashboards
      chart-path: charts/signalizr-dashboards
      has-image: false
      push-chart: true
      chart-testing-cli-command: skip
    secrets: inherit

  deploy:
    needs: [version, package]
    if: github.actor != 'copilot[bot]'
    uses: f2calv/gha-workflows/.github/workflows/gitops-helm-update.yml@v1
    permissions:
      contents: read
      packages: read
    with:
      tag: ${{ needs.version.outputs.chart-version }}
      image-registry: ghcr.io/${{ github.repository_owner }}
      image-repository: signalizr
      chart-registry: ghcr.io/${{ github.repository_owner }}
      chart-registry-username: ${{ github.repository_owner }}
      chart-repository: charts/signalizr-dashboards
      gitops-repo: ${{ vars.GITOPS_REPOSITORY }}
      gitops-repo-update: true
      manifest-paths: ${{ vars.SIGNALIZR_DASHBOARD_MANIFEST_PATH }}
      namespace: ${{ vars.SIGNALIZR_DASHBOARD_NAMESPACE }}
      environment: ${{ vars.SIGNALIZR_DASHBOARD_ENVIRONMENT }}
    secrets: inherit
